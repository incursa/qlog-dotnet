# Schemas

This directory holds repo-local guidance for SpecTrace schema and validation support in `Incursa.Qlog`.

The canonical SpecTrace model schema is published by the `incursa/spec-trace` repository and is consumed remotely by the local validation script.
