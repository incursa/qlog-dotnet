# Templates

This directory holds repo-local guidance for SpecTrace authoring in `Incursa.Qlog`.

The canonical SpecTrace templates remain in the `incursa/spec-trace` repository, but this repository keeps a local place for future qlog-specific template notes if they become necessary.
